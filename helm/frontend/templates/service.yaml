apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: {{ .Values.global.service.frontendPort }}
      targetPort: {{ .Values.global.service.frontendPort }}
  type: ClusterIP
