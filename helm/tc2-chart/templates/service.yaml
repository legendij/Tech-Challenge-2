apiVersion: v1
kind: Service
metadata:
  name: tc2-svc
  namespace: tc2
spec:
  selector:
    app: tc2-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: ClusterIP
