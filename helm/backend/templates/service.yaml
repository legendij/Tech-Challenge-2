apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: {{ .Values.global.service.backendPort }}
      targetPort: {{ .Values.global.service.backendPort }}
  type: ClusterIP
